<section class='subsidiaries_links'>
  <%= link_to 'Registrar novo modelo de carro', new_car_model_path, class: 'link-btn' %>
  <%= link_to 'Voltar', root_path, class: 'link-btn' %>
</section>

<% if @car_models.none? %>
  <h3>Nenhum modelo de carro cadastrado</h3>
<% end %>

<% @car_models.each do |car_model| %>
  <p><%= link_to "#{car_model.name} - #{car_model.year}", car_model %></p>
  <p><%= car_model.year %></p>
  <p><%= car_model.manufacturer %></p>
<% end %>
