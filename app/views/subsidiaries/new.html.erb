<section class='subsidiaries_links'>
  <%= link_to 'Voltar', subsidiaries_path, class: 'link-btn'%>
</section>

<section class='subsidiaries'>
  <%= form_with(model: @subsidiary, local: true, html: {class: 'car_categories_form'}) do |form| %>
    <p>
      <%= form.label :name, 'Nome' %>
      <%= form.text_field :name %>
    </p>

    <p>
      <%= form.label :cnpj, 'CNPJ' %>
      <%= form.text_field :cnpj %>
    </p>

    <p>
      <%= form.label :address, 'Endereço' %>
      <%= form.text_field :address %>
    </p>

    <p>
      <%= form.submit 'Enviar', class: 'link-btn' %>
    </p>
  <% end %>
</section>